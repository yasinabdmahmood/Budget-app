<h1>Transaction#index</h1>
<p>Find me in app/views/transaction/index.html.erb</p>
<div class="container">
<% @records.each do |record| %>
<div  class='d-flex flex-column justify-content-start p-2 my-3 border'>
    <div class="flex-grow-1 px-2">
        <h2><%= record.name%></h2>
        
    </div>
    <div>
        <span>
            <%= record.amount %> $
        </span>
    </div>
    <div>
    <span><%= record.created_at %></span>
    </div>
</div>
<% end %>
<div class="d-flex justify-content-center p-5">
<%= button_to "Create transaction", transaction_new_path, method: :get,class:"btn btn-success" %>
</div>
</div>
